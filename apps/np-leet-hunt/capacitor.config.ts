import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'np.leet.hunt',
    appName: 'np-leet-hunt',
    webDir: '../../dist/apps/np-leet-hunt',
    bundledWebRuntime: false,
};

export default config;
